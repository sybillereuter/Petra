# Node.js / npm / yarn
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # If you exclusively use yarn.lock
# yarn.lock # If you exclusively use package-lock.json (choose one or neither if you use both)


# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Logs
*.log

# Metro
.metro-health-check*
.metro-cache/

# Temporary files and folders
temp/
tmp/

# Bundled app files (generated by 'react-native bundle')
*.bundle
*.js.map
# Source maps, often large and not needed in repo

# Watchman (optional, but good to ignore)
.watchmanconfig

# Android specific (if you manage Android native code)
android/app/build/
android/build/
android/.gradle/
android/gradle/
android/gradlew
android/gradlew.bat
android/local.properties
android/.idea/ # Android Studio/IntelliJ specific for the Android part
android/captures/
android/app/src/main/assets/index.android.bundle # If you bundle manually into assets

# iOS specific (if you manage iOS native code)
ios/build/
ios/Pods/ # CocoaPods dependencies
ios/.xcode.env.local
ios/DerivedData/
*.hmap
*.ipa # iOS app archive
# *.xcworkspace/xcshareddata/ # Often committed, but can be ignored if causing merge conflicts
# *.xcodeproj/project.xcworkspace/ # Often committed
# *.xcodeproj/xcuserdata/ # User-specific Xcode data

# IntelliJ / JetBrains IDE specific
.idea/
*.iml
*.ipr
*.iws
shelf/ # IntelliJ shelf files
tasks.xml # IntelliJ tasks
# workspace.xml # Often committed for shared run configurations, but can be ignored

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem
.AppleDouble
.LSOverride

# local env files
.env*.local

# Thumbs.db (Windows Thumbnail cache)
Thumbs.db

# Build artifacts
build/ # General build folder, might conflict if you have other build systems

# Environment variables
.env
.env.*
!.env.example # Keep example env files

# Test coverage
coverage/

# Optional: Flow type checking
.flowconfig # If you don't want to share flow config
flow-typed/

# Optional: TypeScript
*.tsbuildinfo

package-lock.json

android/
ios/
